#include <stdio.h>
int main() {
     for(int i=1;i<=100;i++){   // using  for loop , to calculate numbers between 1 to 100
       
     if(i%2!=0)               //calculation of  odd numbers between 1 to 100 with the help of using for loop.                              
            printf("%d\n",i);   
    }
  
    return 0;
}
